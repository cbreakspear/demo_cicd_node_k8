apiVersion: v1
kind: Service
metadata:
    name: "myakscluster-a54f"
    labels:
        app: "myakscluster-a54f"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "myakscluster-a54f"